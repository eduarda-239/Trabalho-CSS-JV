var botao = document.getElementById ('botao');

botao.addEventListener('focus', () => {
    botao.style.borderColor = #4A5f6a 
});
botao.addEventListener('blur', () =>{
    botao.style.borderColor = "#ccc";
});